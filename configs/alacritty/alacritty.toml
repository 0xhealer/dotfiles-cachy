# alacritty.toml
# No native blur support in alacritty regardless - opacity only, same
# caveat as kitty.conf about blur being a Niri window-rule.

[window]
opacity = 0.85

[general]
import = ["~/.config/alacritty/colors.toml"]

[font]
size = 12.0
